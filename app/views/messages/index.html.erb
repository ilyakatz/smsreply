Received messages

<table border="1">
  <th>Id</th>
  <th>Date</th>
  <th>Destination</th>
  <th>Origin</th>
  <th>Autoreplied</th>
  <th>Autoreplied message</th>
  <th>Autoreplied text</th>

  <% @received_messages.each do |message| %>
      <tr>
        <td><%= message.id %></td>
        <td><%= I18n.l(message.received, :format=>:long) %></td>
        <td><%= message.destination %></td>
        <td><%= message.origin %></td>
        <td><%= message.replied_on if message.replied_on and message.reply_message_id.nil?%></td>
        <td><%= message.reply_message_id %></td>
        <td><%= message.reply_text %></td>
      </tr>

  <% end %>
</table>

Sent messages

<table border="1">
  <th>Id</th>
  <th>Date</th>
  <th>Destination</th>
  <th>Origin</th>
  <th>Autoreplied</th>
  <th>Autoreplied message</th>
  <th>Autoreplied text</th>

  <% @sent_messages.each do |message| %>
      <tr>
        <td><%= message.id %></td>
        <td><%= I18n.l(message.received, :format=>:long) %></td>
        <td><%= message.destination %></td>
        <td><%= message.origin %></td>
        <td><%= message.replied_on if message.replied_on and message.reply_message_id.nil?%></td>
        <td><%= message.reply_message_id %></td>
        <td><%= message.reply_text %></td>
      </tr>

  <% end %>
</table>



<%= link_to "Get latest data", messages_url, :method=>:post %>  |
<%= link_to "Send replies", replies_url %> |
<%= link_to 'New Away calendar', new_phone_number_setup_away_calendar_path(current_user.phone_number_setups.first) %>